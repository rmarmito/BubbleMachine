// src/components/Timeline.jsx
import React, { useRef } from "react";

const Timeline = () => {
  // Placeholder for timeline functionality
  return <div id="timeline" ref={useRef(null)} />;
};

export default Timeline;
